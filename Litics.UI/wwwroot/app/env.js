(function (window) {
    window.__env = window.__env || {};

    //App Name
    window.__env.productName = 'Litics';

    // API url
    window.__env.Host = 'localhost:8564';
    window.__env.apiUrl = 'http://' + window.__env.Host + '/api';

}(this));